using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LightsOnOff : MonoBehaviour
{ /*

    public Material gray_mat;
    public Material light_mat;
    private bool changeMaterial;

    // Start is called before the first frame update
    void Start()
    {
        changeMaterial = true;
    }

    // Update is called once per frame
    void Update()
    {
        if(Input.GetKeyDown(KeyCode.Space))
        {
            changeColor();
        }
    }

    private void changeColor()
    {
        if(changeMaterial == private void OnRenderImage(RenderTexture src, RenderTexture dest) {
            {
                gray_mat.
            }
        }
    } */
} 
